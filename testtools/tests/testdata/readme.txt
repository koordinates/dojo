Test data for tests in testscripts.

iframe_xmldecl-data.xml is unused.
IE6 goes into quirks mode when it sees the xml declaration,
and anyhow doesn't seem to every allow an XML DOM when loading a page in a browser
(versus with msxml).

