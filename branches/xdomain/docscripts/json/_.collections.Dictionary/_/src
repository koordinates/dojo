var items = {};
this.count = 0;

 dictionary: null

if (dictionary){
	 dictionary: dojo.collections.Dictionary
	var e = dictionary.getIterator();
	while (!e.atEnd) {
		 this.add(e.key, e.value);
		 e.moveNext();
	}
}