// Package ensures consistent order of execution across all implementations

dojo.provide("dojo.html");

// Must have written or evaluated the parser module before
// showing its face.

dojo.require("dojo.parser");
dojo.require("dojo.html-face");
