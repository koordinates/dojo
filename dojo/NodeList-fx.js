// Package ensures consistent order of execution across all implementations

dojo.provide("dojo.NodeList-fx");

// Must have written or evaluated the FX and NodeList modules before
// showing its face.

dojo.require("dojo.fx");
dojo.require("dojo.NodeList-fx-face");