// Package ensures consistent order of execution across all implementations

dojo.provide("dojo.NodeList-html");

// Must have written or evaluated the HTML and NodeList modules before
// showing its face.

dojo.require("dojo.html");
dojo.require("dojo.NodeList-html-face");