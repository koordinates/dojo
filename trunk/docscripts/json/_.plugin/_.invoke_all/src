var result = {};

if (!dojo.plugin._modules) {
  return result;
}

for (var module in dojo.plugin._modules) {
   This should always return an associative array
  result = dojo.lang.mixin(result, dojo.plugin.invoke.call(arguments.caller, arguments));
}

return result;